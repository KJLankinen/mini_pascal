- testaaminen, eli useiden eri pascal ohjelmien kirjoittaminen

-stacker:
    - funktiot, expressionit, variable yms. tarvitsevat tietoa tyypeistä.
    - mappaus scope depthista ja variable nimestä uniikkiin indeksiin, joka vastaa local variablea

- webassembly:
    - var, eli pass-by-reference: frame pointer?

- kirjoitettavaa webassemblykoodia:
    - assert-funktio, joka käyttää unreachablea ja osaa tulostaa expressionin, rivin ja tiedoston.
    - string concatenate
    - array access & assign, jotka tarkistaa, että idx on pienempi kuin pituus
    - read reals, write reals (ehkä integeriä käyttäen).
    - read kaikki
    - string eq, neq, leq, geq, ge, le --> vertaa stringien pituuksia
    - get_string_litera(idx) --> palauttaa string literalin osoitteen indeksin perusteella

- predefined identifiers miettiminen ja lisääminen
